import connection from "../config/connectDB";

export const withdrawHistoryController = () => {
    
    
    try {
        connection.execute(`select `)
       } catch (error) {
        
       }     
};